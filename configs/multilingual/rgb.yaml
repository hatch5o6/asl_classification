# RGB baseline — AUTSL + ASL Citizen + GSL combined (3,267 classes)
# Labels are language-prefixed: autsl_*, asl_*, gsl_*
save: /home/ccoulson/groups/grp_asl_classification/nobackup/archive/SLR/models/multilingual/rgb
test_checkpoint: /home/ccoulson/groups/grp_asl_classification/nobackup/archive/SLR/models/multilingual/rgb/checkpoints/epoch=8-step=13718-val_loss=1.729421-val_acc=0.611891.ckpt
modalities: ["rgb"]
joint_pruning: False

train_csv: data/multilingual/train.csv
val_csv: data/multilingual/val.csv
test_csv: data/multilingual/test.csv
class_id_csv: data/multilingual/class_ids.csv

seed: 4000
num_frames: video_mae
n_gpus: 8
device: cuda

pretrained_learning_rate: 1e-04
new_learning_rate: 2e-04
skel_learning_rate: 2e-04
class_learning_rate: 2e-04
weight_decay: 0.01

effective_batch_size: 64
early_stop: 30
save_top_k: 10
val_interval: 0.25
max_steps: 200000

fusion_dim: 512
depth_image_size: 224
depth_hidden_dim: 384
depth_hidden_layers: 6
depth_att_heads: 6
depth_intermediate_size: 1536
depth_patch_size: 16

bert_hidden_layers: 2
bert_hidden_dim: 512
bert_att_heads: 8
bert_intermediate_size: 2048
bert_dropout: 0.0

num_pose_points: 543
classifier_dropout: 0.2
gradient_clip_val: 1.0
